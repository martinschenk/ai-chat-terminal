# Core dependencies
sentence-transformers>=2.2.0
sqlite-vec>=0.1.0
apsw>=3.45.0

# PII Detection (optional, graceful fallback if missing)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Response generation (optional)
# ollama (external service, not a Python package)
